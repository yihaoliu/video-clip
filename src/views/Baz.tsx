import * as React from "react";
import { Helmet } from "react-helmet";

class Baz extends React.Component {
  public render() {
    return (
      <div>
        <input type="file" />
      </div>
    );
  }
}

export default Baz;
